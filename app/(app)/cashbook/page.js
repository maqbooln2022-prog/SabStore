// TODO(claude-code): port function Cashbook from reference/kirana-store-app.jsx.
// Unified running ledger combining sales, udhaar payments, draws, expenses, and stock purchases with a live balance.
// Replace local React state (useState arrays) with Supabase queries/mutations
// scoped to the active shop -- see supabase/schema.sql for the matching table.

export default function CashbookPage() {
  return <div>Cashbook -- port from reference/kirana-store-app.jsx</div>;
}
