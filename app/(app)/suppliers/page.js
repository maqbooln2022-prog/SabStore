// TODO(claude-code): port function Suppliers from reference/kirana-store-app.jsx.
// Supplier directory (name, phone, what they supply), auto-linked purchase count from movements, WhatsApp messaging.
// Replace local React state (useState arrays) with Supabase queries/mutations
// scoped to the active shop -- see supabase/schema.sql for the matching table.

export default function SuppliersPage() {
  return <div>Suppliers -- port from reference/kirana-store-app.jsx</div>;
}
