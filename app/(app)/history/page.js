// TODO(claude-code): port function History from reference/kirana-store-app.jsx.
// Past bills list, reprint, resend via WhatsApp.
// Replace local React state (useState arrays) with Supabase queries/mutations
// scoped to the active shop -- see supabase/schema.sql for the matching table.

export default function HistoryPage() {
  return <div>History -- port from reference/kirana-store-app.jsx</div>;
}
