// TODO(claude-code): port function CreditBook from reference/kirana-store-app.jsx.
// Udhaar ledger per customer, record payment (with UPI QR), single + bulk WhatsApp reminders (BulkReminderModal).
// Replace local React state (useState arrays) with Supabase queries/mutations
// scoped to the active shop -- see supabase/schema.sql for the matching table.

export default function CreditPage() {
  return <div>Credit -- port from reference/kirana-store-app.jsx</div>;
}
